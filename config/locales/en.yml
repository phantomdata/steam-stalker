en:
  errors:
    steam_api:
      http_error: "There was an error fetching from %{uri} with params %{params}.  The resulting error was %{response_body}"
      json_error: "There was an error parsing the JSON returned by Steam's API.  The JSON in question was %{response_body}"
