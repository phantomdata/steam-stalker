deployment:
  production:
    branch: master
    heroku:
      appname: steam-stalker
      command:
        - heroku run rake db:migrate --app steam-stalker
test:
  override:
    - bundle exec rails test
