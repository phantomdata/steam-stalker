<h2>Sign up</h2>

<p>Feel free to check this app out.  Don't expect it to live forever though, this is a personal side-project of mine.  You can totally fork the repo from <%= link_to 'GitHub', 'https://github.com/phantomdata/steam-stalker' %> if you really find it useful.</p>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= devise_error_messages! %>
  <fieldset class="small-form">

    <div class="field">
      <%= f.label :email %><br />
      <%= f.email_field :email, autofocus: true %>
    </div>

    <div class="field">
      <%= f.label :password %>
      <%= f.password_field :password, autocomplete: "off" %>
      <% if @minimum_password_length %>
        <p class="fine-print"><%= @minimum_password_length %> characters minimum</p>
      <% end %>
    </div>

    <div class="field">
      <%= f.label :password_confirmation %><br />
      <%= f.password_field :password_confirmation, autocomplete: "off" %>
    </div>

    <div class="field">
      <%= f.label :vanity_name %>
      <%= f.text_field :vanity_name %>
      <p class="fine-print">This is effectively your Steam username.  Its what you type when you go to http://steamcommunity.com/id/VANITY_NAME.</p>
    </div>

    <div class="actions">
      <%= f.submit "Sign up", class: 'button' %>
    </div>
  </fieldset>
<% end %>

<%= render "devise/shared/links" %>
